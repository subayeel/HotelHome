import React from "react";
import { Input, StyledSearchBar } from "./styled/Input.styled";

function SearchBar(props) {
  return (
    
      <Input type="text" placeholder="Search"/>
      
  );
}

export default SearchBar;
