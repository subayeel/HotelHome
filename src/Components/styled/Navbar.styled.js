import styled from "styled-components";

export const StyledNavbar = styled.div`
  display: flex;
  align-items: center;
  background: linear-gradient(to right, ${({theme})=> theme.colors.primarybg} 50%, ${({theme})=> theme.colors.secondarybg} 50%);
  
  
   
`;
